declare const __webpack_public_path__: String;

declare type complex = {
  real: number;
  img: number;
};

declare type coord = {
  x: number;
  y: number;
};

declare type colorRGB = {
  red: number;
  green: number;
  blue: number;
};

declare type limit = {
  x: [number, number];
  y: [number, number];
};
